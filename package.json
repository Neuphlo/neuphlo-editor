{
  "name": "neuphlo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "dev": "concurrently -n lib,app -c blue,green \"npm -w neuphlo-editor run dev\" \"npm -w neuphlo-editor-tester run dev\"",
    "clean": "npm run --workspaces --if-present clean || true"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
